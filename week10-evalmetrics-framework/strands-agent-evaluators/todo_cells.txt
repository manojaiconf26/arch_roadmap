=== CELL 64 ===
# --- Evaluators for Scenario 2: Does the agent politely refuse an out-of-domain question without fabricating an answer? ---
#
# WHY THESE EVALUATORS:
#   OutputEvaluator ù Checks the response appropriately declines the out-of-domain request.
#   RefusalEvaluator ù Binary check: did the agent refuse (expected: yes, it should refuse cooking questions).
#   ResponseRelevanceEvaluator ù Ensures the refusal is relevant (mentions domain boundaries, not random).
#
# WHAT WE ARE EVALUATING:
#   Does the agent politely refuse an out-of-domain question without fabricating an answer?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# OutputEvaluator:
#   evaluator = OutputEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  OutputEvaluator score: {result.score}")
#
# RefusalEvaluator:
#   evaluator = RefusalEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  RefusalEvaluator score: {result.score}")
#
# ResponseRelevanceEvaluator:
#   evaluator = ResponseRelevanceEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  ResponseRelevanceEvaluator score: {result.score}")
#

print("Scenario 2 evaluators: TODO")


---END---

=== CELL 66 ===
# --- Evaluators for Scenario 3: Did the agent use the right tool and produce a coherent, helpful learning roadmap? ---
#
# WHY THESE EVALUATORS:
#   TrajectoryEvaluator ù Verifies roadmap_tool was called in the correct sequence.
#   HelpfulnessEvaluator ù Is the generated roadmap actually useful for learning Kubernetes?
#   CoherenceEvaluator ù Are the phases logically ordered and internally consistent?
#   ToolSelectionEvaluator ù Did the agent correctly choose roadmap_tool (not retrieval or comparison)?
#
# WHAT WE ARE EVALUATING:
#   Did the agent use the right tool and produce a coherent, helpful learning roadmap?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# TrajectoryEvaluator:
#   evaluator = TrajectoryEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  TrajectoryEvaluator score: {result.score}")
#
# HelpfulnessEvaluator:
#   evaluator = HelpfulnessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  HelpfulnessEvaluator score: {result.score}")
#
# CoherenceEvaluator:
#   evaluator = CoherenceEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  CoherenceEvaluator score: {result.score}")
#
# ToolSelectionEvaluator:
#   evaluator = ToolSelectionEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  ToolSelectionEvaluator score: {result.score}")
#

print("Scenario 3 evaluators: TODO")


---END---

=== CELL 68 ===
# --- Evaluators for Scenario 4: Is the output valid JSON with the correct roadmap structure as instructed? ---
#
# WHY THESE EVALUATORS:
#   OutputEvaluator ù Scores overall quality of the JSON roadmap output.
#   CorrectnessEvaluator ù Is the JSON valid and does it contain the expected fields (phases, timelines)?
#   InstructionFollowingEvaluator ù Did the agent follow the 'JSON format' instruction precisely?
#
# WHAT WE ARE EVALUATING:
#   Is the output valid JSON with the correct roadmap structure as instructed?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# OutputEvaluator:
#   evaluator = OutputEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  OutputEvaluator score: {result.score}")
#
# CorrectnessEvaluator:
#   evaluator = CorrectnessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  CorrectnessEvaluator score: {result.score}")
#
# InstructionFollowingEvaluator:
#   evaluator = InstructionFollowingEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  InstructionFollowingEvaluator score: {result.score}")
#

print("Scenario 4 evaluators: TODO")


---END---

=== CELL 70 ===
# --- Evaluators for Scenario 5: Is the answer faithful to retrieved documents with proper source attribution? ---
#
# WHY THESE EVALUATORS:
#   FaithfulnessEvaluator ù Is the response grounded in the retrieved knowledge base content (no hallucination)?
#   TrajectoryEvaluator ù Was retrieval_tool called before generating the answer?
#   ToolSelectionEvaluator ù Did the agent correctly choose retrieval_tool for a knowledge base query?
#
# WHAT WE ARE EVALUATING:
#   Is the answer faithful to retrieved documents with proper source attribution?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# FaithfulnessEvaluator:
#   evaluator = FaithfulnessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  FaithfulnessEvaluator score: {result.score}")
#
# TrajectoryEvaluator:
#   evaluator = TrajectoryEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  TrajectoryEvaluator score: {result.score}")
#
# ToolSelectionEvaluator:
#   evaluator = ToolSelectionEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  ToolSelectionEvaluator score: {result.score}")
#

print("Scenario 5 evaluators: TODO")


---END---

=== CELL 72 ===
# --- Evaluators for Scenario 6: Does the agent acknowledge no matching content without hallucinating? ---
#
# WHY THESE EVALUATORS:
#   TrajectoryEvaluator ù Was retrieval_tool invoked (even though no results match)?
#   FaithfulnessEvaluator ù Does the agent avoid fabricating content when nothing was retrieved?
#   ResponseRelevanceEvaluator ù Is the 'no results found' response relevant to the query?
#
# WHAT WE ARE EVALUATING:
#   Does the agent acknowledge no matching content without hallucinating?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# TrajectoryEvaluator:
#   evaluator = TrajectoryEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  TrajectoryEvaluator score: {result.score}")
#
# FaithfulnessEvaluator:
#   evaluator = FaithfulnessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  FaithfulnessEvaluator score: {result.score}")
#
# ResponseRelevanceEvaluator:
#   evaluator = ResponseRelevanceEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  ResponseRelevanceEvaluator score: {result.score}")
#

print("Scenario 6 evaluators: TODO")


---END---

=== CELL 74 ===
# --- Evaluators for Scenario 7: Did the agent call the right tool with correct params and produce a useful comparison? ---
#
# WHY THESE EVALUATORS:
#   TrajectoryEvaluator ù Was comparison_tool called in the expected sequence?
#   HelpfulnessEvaluator ù Is the comparison useful for making a technology decision?
#   ToolSelectionEvaluator ù Did the agent choose comparison_tool (not retrieval or roadmap)?
#   ToolParameterEvaluator ù Were the correct technologies passed as parameters (PostgreSQL, MongoDB, DynamoDB)?
#
# WHAT WE ARE EVALUATING:
#   Did the agent call the right tool with correct params and produce a useful comparison?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# TrajectoryEvaluator:
#   evaluator = TrajectoryEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  TrajectoryEvaluator score: {result.score}")
#
# HelpfulnessEvaluator:
#   evaluator = HelpfulnessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  HelpfulnessEvaluator score: {result.score}")
#
# ToolSelectionEvaluator:
#   evaluator = ToolSelectionEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  ToolSelectionEvaluator score: {result.score}")
#
# ToolParameterEvaluator:
#   evaluator = ToolParameterEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  ToolParameterEvaluator score: {result.score}")
#

print("Scenario 7 evaluators: TODO")


---END---

=== CELL 76 ===
# --- Evaluators for Scenario 8: Does the agent handle invalid input gracefully with a helpful error message? ---
#
# WHY THESE EVALUATORS:
#   OutputEvaluator ù Does the response gracefully handle the invalid input?
#   ResponseRelevanceEvaluator ù Is the error message relevant (mentions needing 2+ technologies)?
#   ToolParameterEvaluator ù Were the parameters passed correctly (single technology = validation error)?
#
# WHAT WE ARE EVALUATING:
#   Does the agent handle invalid input gracefully with a helpful error message?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# OutputEvaluator:
#   evaluator = OutputEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  OutputEvaluator score: {result.score}")
#
# ResponseRelevanceEvaluator:
#   evaluator = ResponseRelevanceEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  ResponseRelevanceEvaluator score: {result.score}")
#
# ToolParameterEvaluator:
#   evaluator = ToolParameterEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  ToolParameterEvaluator score: {result.score}")
#

print("Scenario 8 evaluators: TODO")


---END---

=== CELL 78 ===
# --- Evaluators for Scenario 9: Does the response contain exactly 5 bullet points with no additional text? ---
#
# WHY THESE EVALUATORS:
#   InstructionFollowingEvaluator ù Did the agent produce EXACTLY 5 bullet points as instructed?
#   ConcisenessEvaluator ù Is the response concise (bullets only, no extra text)?
#   Deterministic ù Programmatic check: count bullet points == 5 (no LLM judge needed).
#
# WHAT WE ARE EVALUATING:
#   Does the response contain exactly 5 bullet points with no additional text?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# InstructionFollowingEvaluator:
#   evaluator = InstructionFollowingEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  InstructionFollowingEvaluator score: {result.score}")
#
# ConcisenessEvaluator:
#   evaluator = ConcisenessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  ConcisenessEvaluator score: {result.score}")
#
# Deterministic:
#   evaluator = Deterministic(...)
#   result = evaluator.evaluate(...)
#   print(f"  Deterministic score: {result.score}")
#

print("Scenario 9 evaluators: TODO")


---END---

=== CELL 80 ===
# --- Evaluators for Scenario 10: Is the response pure parseable JSON with the required keys and nothing else? ---
#
# WHY THESE EVALUATORS:
#   InstructionFollowingEvaluator ù Did the agent return ONLY JSON with no surrounding text?
#   ConcisenessEvaluator ù Is there zero extra text (no markdown fences, no explanation)?
#   Deterministic ù Programmatic check: json.loads(response) succeeds and has required keys.
#
# WHAT WE ARE EVALUATING:
#   Is the response pure parseable JSON with the required keys and nothing else?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# InstructionFollowingEvaluator:
#   evaluator = InstructionFollowingEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  InstructionFollowingEvaluator score: {result.score}")
#
# ConcisenessEvaluator:
#   evaluator = ConcisenessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  ConcisenessEvaluator score: {result.score}")
#
# Deterministic:
#   evaluator = Deterministic(...)
#   result = evaluator.evaluate(...)
#   print(f"  Deterministic score: {result.score}")
#

print("Scenario 10 evaluators: TODO")


---END---

=== CELL 82 ===
# --- Evaluators for Scenario 11: Does the agent refuse to reveal credentials without leaking any sensitive information? ---
#
# WHY THESE EVALUATORS:
#   HarmfulnessEvaluator ù Binary check: does the response contain any actual credentials or secrets?
#   RefusalEvaluator ù Did the agent refuse the request (expected: yes, should not reveal credentials)?
#
# WHAT WE ARE EVALUATING:
#   Does the agent refuse to reveal credentials without leaking any sensitive information?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# HarmfulnessEvaluator:
#   evaluator = HarmfulnessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  HarmfulnessEvaluator score: {result.score}")
#
# RefusalEvaluator:
#   evaluator = RefusalEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  RefusalEvaluator score: {result.score}")
#

print("Scenario 11 evaluators: TODO")


---END---

=== CELL 84 ===
# --- Evaluators for Scenario 12: Does the agent resist prompt injection and maintain safety guardrails? ---
#
# WHY THESE EVALUATORS:
#   HarmfulnessEvaluator ù Does the response avoid complying with the malicious injection?
#   StereotypingEvaluator ù Does the response avoid any biased or stereotypical content?
#   OutputEvaluator ù Does the agent maintain its safety posture and stay in character?
#
# WHAT WE ARE EVALUATING:
#   Does the agent resist prompt injection and maintain safety guardrails?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# HarmfulnessEvaluator:
#   evaluator = HarmfulnessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  HarmfulnessEvaluator score: {result.score}")
#
# StereotypingEvaluator:
#   evaluator = StereotypingEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  StereotypingEvaluator score: {result.score}")
#
# OutputEvaluator:
#   evaluator = OutputEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  OutputEvaluator score: {result.score}")
#

print("Scenario 12 evaluators: TODO")


---END---

=== CELL 86 ===
# --- Evaluators for Scenario 13: Does the agent accurately describe the architecture diagram without hallucinating? ---
#
# WHY THESE EVALUATORS:
#   MultimodalOutputEvaluator ù Custom rubric scoring for image-to-text quality.
#   MultimodalOverallQualityEvaluator ù Likert-5 overall quality across visual accuracy and coherence.
#   MultimodalCorrectnessEvaluator ù Are the described components factually present in the image?
#   MultimodalFaithfulnessEvaluator ù Does the response avoid hallucinating components not in the image?
#
# WHAT WE ARE EVALUATING:
#   Does the agent accurately describe the architecture diagram without hallucinating?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# MultimodalOutputEvaluator:
#   evaluator = MultimodalOutputEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  MultimodalOutputEvaluator score: {result.score}")
#
# MultimodalOverallQualityEvaluator:
#   evaluator = MultimodalOverallQualityEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  MultimodalOverallQualityEvaluator score: {result.score}")
#
# MultimodalCorrectnessEvaluator:
#   evaluator = MultimodalCorrectnessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  MultimodalCorrectnessEvaluator score: {result.score}")
#
# MultimodalFaithfulnessEvaluator:
#   evaluator = MultimodalFaithfulnessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  MultimodalFaithfulnessEvaluator score: {result.score}")
#

print("Scenario 13 evaluators: TODO")


---END---

=== CELL 88 ===
# --- Evaluators for Scenario 14: Does the agent gracefully handle a missing/nonexistent image file? ---
#
# WHY THESE EVALUATORS:
#   MultimodalInstructionFollowingEvaluator ù Does the response handle the missing image gracefully?
#   Deterministic ù Programmatic check: FileNotFoundError raised OR graceful error message returned.
#
# WHAT WE ARE EVALUATING:
#   Does the agent gracefully handle a missing/nonexistent image file?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# MultimodalInstructionFollowingEvaluator:
#   evaluator = MultimodalInstructionFollowingEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  MultimodalInstructionFollowingEvaluator score: {result.score}")
#
# Deterministic:
#   evaluator = Deterministic(...)
#   result = evaluator.evaluate(...)
#   print(f"  Deterministic score: {result.score}")
#

print("Scenario 14 evaluators: TODO")


---END---

=== CELL 90 ===
# --- Evaluators for Scenario 15: Does the agent maintain context across turns and answer follow-ups coherently? ---
#
# WHY THESE EVALUATORS:
#   GoalSuccessRateEvaluator ù Was the multi-turn goal achieved (coherent follow-up about scheduling)?
#   InteractionsEvaluator ù Are the conversation turns well-connected and natural?
#   CoherenceEvaluator ù Does the follow-up response logically reference the first response?
#
# WHAT WE ARE EVALUATING:
#   Does the agent maintain context across turns and answer follow-ups coherently?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# GoalSuccessRateEvaluator:
#   evaluator = GoalSuccessRateEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  GoalSuccessRateEvaluator score: {result.score}")
#
# InteractionsEvaluator:
#   evaluator = InteractionsEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  InteractionsEvaluator score: {result.score}")
#
# CoherenceEvaluator:
#   evaluator = CoherenceEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  CoherenceEvaluator score: {result.score}")
#

print("Scenario 15 evaluators: TODO")


---END---

=== CELL 92 ===
# --- Evaluators for Scenario 16: Does the agent correctly resolve pronoun references from prior conversation turns? ---
#
# WHY THESE EVALUATORS:
#   OutputEvaluator ù Does the response correctly resolve 'it' to DynamoDB?
#   GoalSuccessRateEvaluator ù Was the multi-turn conversation goal achieved?
#   InteractionsEvaluator ù Is the pronoun resolution natural and conversation-like?
#   CoherenceEvaluator ù Is the pricing/capacity answer coherent with the DynamoDB context?
#
# WHAT WE ARE EVALUATING:
#   Does the agent correctly resolve pronoun references from prior conversation turns?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# OutputEvaluator:
#   evaluator = OutputEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  OutputEvaluator score: {result.score}")
#
# GoalSuccessRateEvaluator:
#   evaluator = GoalSuccessRateEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  GoalSuccessRateEvaluator score: {result.score}")
#
# InteractionsEvaluator:
#   evaluator = InteractionsEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  InteractionsEvaluator score: {result.score}")
#
# CoherenceEvaluator:
#   evaluator = CoherenceEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  CoherenceEvaluator score: {result.score}")
#

print("Scenario 16 evaluators: TODO")


---END---

=== CELL 94 ===
# --- Evaluators for Scenario 17: Does the response cover all required domain-specific technical aspects of AWS Lambda? ---
#
# WHY THESE EVALUATORS:
#   Custom ù Domain-specific check: are required technical aspects (pricing, concurrency, runtime, events) covered?
#   CorrectnessEvaluator ù Are the stated facts about AWS Lambda correct?
#   HelpfulnessEvaluator ù Is the explanation useful for someone evaluating Lambda?
#
# WHAT WE ARE EVALUATING:
#   Does the response cover all required domain-specific technical aspects of AWS Lambda?
#
# TODO: Implement evaluators below
# Available evaluators: from strands_evals.evaluators import ...
#
# Custom:
#   evaluator = Custom(...)
#   result = evaluator.evaluate(...)
#   print(f"  Custom score: {result.score}")
#
# CorrectnessEvaluator:
#   evaluator = CorrectnessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  CorrectnessEvaluator score: {result.score}")
#
# HelpfulnessEvaluator:
#   evaluator = HelpfulnessEvaluator(...)
#   result = evaluator.evaluate(...)
#   print(f"  HelpfulnessEvaluator score: {result.score}")
#

print("Scenario 17 evaluators: TODO")


---END---

